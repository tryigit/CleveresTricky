I need to check `WebServer.kt.orig` again, because `WebServer.kt` has `isValidPkg` but `WebServer.kt.orig` DOES NOT have `isValidPkg` and uses `.matches()`.
Wait, why is there a `.orig` file? Ah! I probably ran some command that created it, maybe? No, it was already there in `ls -la`.
Wait!
Let's see the differences between `WebServer.kt` and `WebServer.kt.orig`.
